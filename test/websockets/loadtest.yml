config:
  target: "ws://localhost:3030/networkRoom"
  socketio:
    transports: ["websocket"]
  ensure:
    maxErrorRate: 1
  phases:
    - duration: 10
      arrivalRate: 20
      rampTo: 20
      name: "Warming up"

scenarios:
  - engine: "socketio"
    flow:
      - emit:
          channel: "requestRoom"


