config:
  target: "ws://localhost:3030"
  socketio:
    transports: ["websocket"]
  ensure:
    maxErrorRate: 1
  phases:
    - duration: 10
      arrivalRate: 350
      rampTo: 400
      name: "Warming up"

scenarios:
  - engine: "socketio"
    flow:
      - emit:
          channel: "requestRoom"


