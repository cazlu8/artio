config:
  target: "ws://localhost:3030"
  socketio:
    transports: ["websocket"]
  ensure:
    maxErrorRate: 1
  phases:
    - duration: 1
      arrivalRate: 1000
      rampTo: 2000
      name: "Warming up"

scenarios:
  - engine: "socketio"
    flow:
      - emit:
          channel: "requestRoom"


