config:
  target: "ws://localhost:3030"
  ensure:
    maxErrorRate: 1
  phases:
    - duration: 5
      arrivalRate: 20
      rampTo: 20
      name: "Warming up"

scenarios:
  - engine: "ws"
    flow:
      - send:
          event: "requestRoom"
